spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/devcorp
    username: test
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
